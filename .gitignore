# ── .NET build output ──────────────────────────────────────────
bin/
obj/
Library/

# ── Visual Studio ──────────────────────────────────────────────
.vs/
*.user

# ── UI (Node / webpack) ────────────────────────────────────────
Pinboard/UI/node_modules/
# Ship the prebuilt bundle, ignore the rest of dist/
Pinboard/UI/dist/*
!Pinboard/UI/dist/Pinboard.mjs

# ── Tooling / local settings ───────────────────────────────────
.claude/

# ── CS2 mod save data (never lives here, but guard anyway) ──────
favourites.json
favourites.json.bak
favourites.json.tmp
